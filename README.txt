When running the script do it by executing certaing lines in "LINE BY LINE" mode generally with 'SHIFT + ENTER'
Depending on which dataset you want to generate, execute those coresponding lines of script.
Dont forget the imports.